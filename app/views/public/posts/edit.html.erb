
<h1>編集画面</h1>

<%= form_with model: @post, url: post_path(@post.id), method: :patch do |f| %>

  <h4>タイトル</h4>
  <%= f.text_field :name %>

  <h4>本文</h4>
  <%= f.text_area :introduction %>

  <%= f.submit '保存' %>

<% end %>